# Media Assets

This document records the project-specific media assets prepared for README and
repository documentation work.

## Screenshots

- `assets/screenshots/toy-inventory-page.png`

## Notes

- The screenshot was captured from the local frontend running in
  `VITE_LOCAL=true` mode to avoid exposing private backend data.
- The filename uses lowercase kebab-case for README-ready reuse.
- The screenshot contains only demo toy data generated by the local in-memory
  service.

## Template Media Cleanup

- Replaced the Vite template favicon with `public/mistertoy-favicon.svg`.
- Removed the unused React template asset `src/assets/react.svg`.
